# Dependencias del servidor MCP unificado (mínimas y estables)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# Cliente HTTP del grupo de API Testing & QA (build_and_send_request,
# run_postman_collection, set_api_auth con OAuth2...).
httpx>=0.27,<1.0

# NOTA: el motor JSONPath y el validador de JSON Schema Draft-07 están
# implementados dentro del propio proyecto (server/api/), por lo que NO se
# necesitan 'jsonpath-ng' ni 'jsonschema'.
#
# 'newman' (runner oficial de colecciones Postman) es OPCIONAL y es un paquete
# de Node, no de Python:   npm install -g newman
# Sin él, run_postman_collection usa el runner nativo incluido.

# Automatización de navegador (grupo de tools browser_*): OPCIONAL.
# Está en requirements-browser.txt porque descarga ~150 MB de binarios.
# Instálalo con:  pip install -r requirements-browser.txt && python -m playwright install chromium
# o arranca con:  ENABLE_BROWSER=true ./start.sh
